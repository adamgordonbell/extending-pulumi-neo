# Demo assets

Runnable code lives in the repo it operates on:
[`adamgordonbell/neo-workshop-incident`](https://github.com/adamgordonbell/neo-workshop-incident)
— the incident program, the trigger/cleanup/prewarm scripts, and the
`esc-readonly-role/` program. Fork it; Neo's fix PR lands on your fork.
(A working checkout sits at `pulumi-ts/`, gitignored.)

Here: [`context-api/`](context-api/) — the read-only graph queries for part 1.
The walkthrough itself is [`../demo.md`](../demo.md).
