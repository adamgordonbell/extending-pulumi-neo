# --- APM ---
# Source of truth is committed: apm.yml, apm.lock.yaml, and .apm/.
# Everything below is generated by `apm install` / `apm compile` and is regenerated.
apm_modules/
.claude/
.mcp.json
.lsp.json
/AGENTS.md
/CLAUDE.md

# --- Node / Slidev (slides/) ---
node_modules/
dist/
.slidev/
*.log

# --- OS / editor noise ---
.DS_Store
.idea/
.vscode/
extracted-skills/

# --- Internal, never published ---
# This repo ships to attendees (QR on the closing slide). Pre-release and
# internal-Slack detail goes here and stays out of git.
INTERNAL.md
demo/pulumi-ts/
